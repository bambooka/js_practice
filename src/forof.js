//code with loop for..in 
//will be showed only key instead value

let browsers = ['Chrome', 'Opera', 'Safari', 'Edge'];

for(let browser in browsers) {
    console.log(browser);
}

// code with loop for..of
//will be showed value

let browsersOf = ['Chrome', 'Opera', 'Safari', 'Edge'];

for (let browser of browsersOf) {
    console.log(browser);
}